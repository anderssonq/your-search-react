import React from 'react'
import { InputSearchStyled } from '../templates/InputSearch'

const InputSearch = ({...rest}) => {
    return (
        <InputSearchStyled {...rest}/>
    )
}

export default InputSearch
